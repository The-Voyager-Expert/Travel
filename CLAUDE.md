# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Travel — entry point for Claude

---

## ⚡ READ THIS FIRST — NO ASKING, EVER

**🔑 Three non-negotiables — every session, from the very start:**

1. **Connected?** Before anything else, confirm the Drive is mounted — the mounted `Travel/` path must be reachable with the Read tool (this file loading is the first proof). If it's unreachable (folder not connected), **STOP and ask in one line right away** — *"The Travel folder isn't connected this session — connect it and I'll pick up."* Don't start the ritual, don't build, don't create files. This is the one blocker that always pauses for a one-line ask; it is flagging a hard blocker, not permission-asking. (Full rule: `Rules for Claude.html § 1` Step 0.)
2. **Edit in place.** To change a file that already exists, use the Edit/Write tool on its mounted path. NEVER use Drive MCP `create_file`/upload on an existing file — it spawns a `Name (1).ext` duplicate and the edit is lost (this lost two cribs). `create_file` is for genuinely new files only. (Full rule: `Rules for Claude.html § 2`.)
3. **Delete tracked, archive untracked.** "delete / remove / clean up / get rid of": a **git-tracked** file (all of `Travel-Website/` + tracked `Brain/` files) → delete it directly — git history is the archive (recover with `git checkout`/`git revert`). A file **git does not track** (Drive-only: `Travel/archive/`, `Hotel Research/`, `On Demand/`, untracked `Brain/Reference/*`, scratch) → move to `Travel/archive/`; git can't recover it. Unsure → `git ls-files --error-unmatch <file>` (tracked = safe to delete). Never delete or archive an incomplete guide build regardless. (Full rule: `Rules for Claude.html § 2`.)

**All tool use is pre-authorized for the entire session.**
Web searches, URL fetches on any domain, Wikipedia, Wikimedia Commons, Chrome MCP, any connector (Viator, TripAdvisor, Booking, Resy, StubHub, Expedia, any), file reads, script runs — execute immediately. Never ask, never confirm, never announce intent and wait for a reply. No URL requires per-site authorization. **Exactly two things get a one-line message** (plain text, never the AskUserQuestion popup): (a) the Drive isn't connected — ask to connect it before doing anything (see Three non-negotiables above); (b) deleting or moving an **untracked** (Drive-only) non-build file, or another genuinely irreversible action such as publishing — a one-line confirm. Deleting a **git-tracked** file is recoverable via git, so it needs no confirm. Nothing else pauses.

**Where the pre-authorization actually lives (so cribs don't re-ask).**
This prose does NOT drive the harness permission prompts — only settings files do. The durable allow-list lives in user-global `~/.claude/settings.json` (read by every session, main or crib, in any directory). Do NOT rely on `.claude/settings.local.json`: it is per-directory AND gitignored, so approvals there never reach worktree cribs and the lists silently drift apart — the classic "cribs re-ask for everything" bug. Never approve a rule containing a literal session scratchpad path (`/private/tmp/claude-501/.../<UUID>/scratchpad/...`) — the UUID changes each session so it re-prompts forever; the glob `Bash(python3 /private/tmp/claude-*/**/scratchpad/*)` covers them all. If cribs start re-asking, re-merge every `.claude/settings.local.json` (root + worktrees) into global. Full write-up in memory: `project_crib_permission_drift`.

**Guide builds require zero clarification.**
When asked to build a guide, all requirements are fully specified in the Brain files. Begin immediately. Run the full build — ritual → reads → research → build → validate → ship — without stopping. Never wait for confirmation.

**Guide builds — HARD GATE: zero HTML before reads.**
Before writing a single line of guide HTML, read in order:
1. `Brain/CORE RULES/Rules for Claude.html`
2. `Brain/CORE RULES/Guide Structure.html`
3. All Phase 1–2 files listed in the Guide build phases section below

Then run: `python3 Brain/scripts/guide_tools.py preflight <City>` — must exit 0 before any HTML is written.

The format lives in `Brain/CORE RULES/` — never in memory, never in past guides. Writing HTML before these reads = wrong format. Self-diagnostic: HTML exists and Phase 0–2 boxes are unchecked in `_build/build_state.md` → stop, delete the HTML, do the reads, re-run preflight, restart from scratch.

**These phrases are banned. Any of these in a draft = stop, delete, act instead:**
- "Want me to…" / "Should I…" / "Shall I…"
- "Would you like me to…" / "Do you want me to…"
- "Let me know if you want me to…"
- "Happy to… if you'd like" / "I can… if that helps"
- "Just say the word and I'll…" / "Ready when you are"
- "Want me to fetch the rules doc?" / "Should I read the brain first?"

**Three moves replace permission-asking:**
1. Do it and announce briefly — "Reading Platforms.md…" then do it.
2. Surface a real fork — "Two paths: A keeps X, B drops it. Which?" (expects a choice, not "yes")
3. Confirm destructive/irreversible only — "About to publish X — confirm?"

**Self-diagnostic:** banned phrase in draft = violation. Delete it. Take the action.

---

## Source of truth

`Brain/CORE RULES/Rules for Claude.html` — governs everything. When this file and Rules for Claude.html disagree, Rules for Claude.html wins. Read it at every session start with the `Read` tool directly — no Drive MCP, no doc_id.

**Google Drive is the ONLY source for rules and guides.** Never fetch `dbellinello.github.io` or any GitHub URL for rules, guides, or structure — the GitHub copies are not the source of truth and fetching them triggers permission pop-ups. GitHub is accessed only when Dani explicitly requests it in the current message.

---

## Session ritual

Runs before the first response, every session. Auto-authorized — no asking.

1. `python3 Brain/scripts/guide_tools.py start` — runs brain-check + sweep-stray + mobile-check (audit) + surfaces open To Do items. On brain-check failures: fix before any task work. On strays: run `--apply` automatically, note in opening message. On mobile-check misses (a shareable page missing the viewport tag or `assets/mobile.css` baseline): run `python3 Brain/scripts/mobile_check.py --apply` automatically, note in opening message.
2. Read `Travel/CLAUDE.md`.
3. Read `Brain/Reference/Brain.md` (Part 1 — Travel Folder Map) — the complete map of every folder, file, and resource available.
4. Read `Brain/CORE RULES/Rules for Claude.html`.
5. Check `Brain/Reference/Platforms.md` — note any ❌ or ⏳ in opening message, do not block.
6. Read `Brain/Reference/Connectors.html` — know what's available, do not announce or prompt.
7. Check `Brain/Reference/audit_log.md` — if last entry > 7 days ago, note in opening message: "Last audit: {date} ({N} days ago)." Continue immediately.

**Two-track ritual — choose based on whether the session will write guide HTML:**
- **Non-build sessions** (flights, hotel research, to-do updates, shopping, on-demand docs, audits): run steps 1–4 + step 7 (audit log). Only steps 5–6 (Platforms, Connectors) are deferred until needed. **Step 4 — reading the behavior doc — is unconditional, every session.**
- **Guide-build sessions** (any session that will write guide HTML): run all 7 steps — no shortcut. Also read `Travel/GUIDE_DEPLOYMENT_PROTOCOL_v1.md` before writing any guide HTML — it documents the 3-layer anti-fake-stamp enforcement and the three problem guides (Pensacola, Naples FL, Naples Italy) that require rebuild. The Phase 0–2 reads (§ Guide build phases) are **hard-gated**: a PreToolUse hook (`Brain/scripts/guide_read_gate.py`, wired in `Travel/.claude/settings.json`) blocks the first write to any `Guides/<City>/*.html` until this session's tool history shows the required `Read` calls (`Rules for Claude.html` for any edit; plus `Guide Structure.html` for a new build). "Begin immediately" never means "skip the reads." The only entire-skip is an explicit non-Travel session; "already ran" only holds when *this* session's tool history proves it — not another crib's, not an assumption.

---

## Routing

| Task | Go to |
|------|-------|
| Build a guide | CLAUDE.md § Guide build phases — Phase 0–2 reads first, then preflight, then build |
| Trip data update | `Travel-Website/Trip-Essentials/Trips.html` — read `Travel-Website/Trip-Essentials/Trips-Rules.md` first |
| Flights / hotels / rentals / weather | Read matching file in `Travel/On Demand/` first |
| Shopping request | Read `Travel/shopping_profile_v2.md` first |
| URL failure in a build | `Brain/CORE RULES/Links.html` |
| PDF rendering | `Brain/Reference/PDF Render Notes.md` |
| Connector capabilities | `Brain/Reference/Connectors.html` |
| Validator work | Read `Rules for Claude.html § 4` before touching anything |

**File location:** all Cowork files stay inside `Travel/`. Mobile surface files in `Travel/On The Go/`.
**Delete / Archive:** git-tracked file → delete freely (`rm`; git recovers it). Untracked / Drive-only file → move to `Travel/archive/` (git can't recover it). Pre-authorized — no asking.
**CORE RULES:** never edit `Brain/CORE RULES/` without explicit per-session approval.
**Guides/:** frozen. Never read, grep, or reference any file there unless explicitly named.
**Connectors:** already configured. Do not prompt to connect, suggest connectors, or search the registry.
**Vocabulary:** when the user says "the HTML," "fix the {name} HTML" → means `Brain/CORE RULES/{name}.html`.

---

## Guide build phases

Full spec in `Brain/CORE RULES/Guide Structure.html`. **Do not write a single line of HTML before Phase 0–2 reads are done. Run `Brain/scripts/guide_tools.py preflight <City>` — must exit 0 before first HTML line.**

**First action of any build — create the build-state tracker:**
`python3 Brain/scripts/guide_tools.py init <City>` — creates `Travel-Website/Guides/{City}/_build/build_state.md` with all Phase 0–6 checkboxes unchecked. Flip each to `[x]` when read or completed. The validator and ship gate read this file — an unchecked Phase 6 = not shipped.

- **Phase 0** — `Rules for Claude.html` (session-start read — always done first)
- **Phase 1** — `Links.html` · `Photos Rules.html` · `Brain/Reference/Connectors.html` · `Brain/Reference/Platforms.md`
- **Phase 2** — `Guide Structure.html` · `Stops Structure.html` · `Hotel Banner.html` · `Trip Overview.html` · `Brain/Reference/Toolbar.html` · `Brain/Reference/Navigation.html`
- **Phase 3** — `Day Structure.html` (before locking any day)
- **Phase 4** — `Tickets.html` · `Motion Rule.html` · `Icon Order and Format.html` (per stop)
- **Phase 5** — matching `*Extra Section*.html` · `Motion Rule.html` + `Icon Order and Format.html` when the section has walk/ride rows (re-read at start of each Extra Section build) · `Brain/Reference/Read-About-Pages.html` (the mandatory Read About companion page)
- **Phase 6** — `Brain/Reference/Ship Checklist.html` · validator 0 failures · ship gate

**City name only** → look in `Trips.html`, use hotel + dates there.
**City + day count** → skip Trips.html, run hotel research, build for stated day count.
Dates never ship in a guide. Always Day 1 / Day 2 / Day N. Never ask for dates.

---

## Research workflow — follow this order, every build

**Before researching anything** — Phase 1 reads must be done: `Links.html` · `Photos Rules.html` · `Brain/Reference/Connectors.html` · `Brain/Reference/Platforms.md`. These define the tools and rules. Skipping them = building without a method.

**Tours (always MCP first — never start with web search):**
1. Viator MCP: `search_experiences` → `get_experience_details`. This is always step 1.
2. GetYourGuide: `site:getyourguide.com {city} {attraction} tour` (no MCP connector exists)
3. TripAdvisor MCP: `search_experiences`
- Bar: 4.5+★ · ≥6 reviews. Full rules in `Tours - Extra Section.html`.

**Photos (Wikimedia Commons only — one source, one method):**
1. Find the filename: WebSearch `site:commons.wikimedia.org {stop name} {city}`
2. Resolve the URL: `python3 Brain/scripts/commons_photo.py "File:{filename}"`
3. Never direct-fetch commons.wikimedia.org — it's blocked. Never use Google Images or Unsplash.

**Links and verification:**
- Every URL live-verified before it ships — including every edit inside a session.
- Platforms marked ⚡ or ❌ in `Platforms.md`: skip web_fetch entirely → `site:{domain}` WebSearch.
- When web_fetch fails on anything else → Chrome MCP (`navigate` + `get_page_text`) immediately.
- Never ask "may I access {domain}?" — pre-authorized, execute.

**Stop research (trusted sources only):**
- Wikipedia (`en.wikipedia.org`) · Fodor's · Culture Trip · Rick Steves · National Geographic Travel · Rough Guides · Atlas Obscura · official tourism boards
- No random blogs, no affiliate lists, no AI-generated SEO content, no content farms — regardless of Google ranking.
- Check `Brain/Reference/Brain.md` for the city before picking any stop.

---

## Behavioral rules

Full detail in `Brain/CORE RULES/Rules for Claude.html` § 3. The short version is at the top of this file. Key points:

- No preamble, no option menus, no pop-up questions. Pick what the rules point to and move.
- Decisive over hesitant. When the task scope is clear, run end-to-end.
- No permission-asking on already-authorized actions. Do it and announce briefly.
- Connector usage stays authorized across the session — no re-asking on every action.
- "Delete" / "remove" / "clean up": git-tracked file → delete it (git history is the archive); untracked / Drive-only file → move to `Travel/archive/`.

---

## ⚠️ DriftyCat — things that keep breaking

One-line tripwires. Full rule for each in its CORE RULES HTML file.

- ⚠️ **Preflight before first HTML — no exceptions.** Run `python3 Brain/scripts/guide_tools.py preflight <City>` before writing any guide HTML. Non-zero exit = unchecked Phase 0–2 reads. Complete them, re-run, proceed only on exit 0. Full rule: CLAUDE.md § Guide build phases.
- ⚠️ **Working-surface drift = fix immediately, no approval.** Any file outside `Brain/CORE RULES/` that drifts from a CORE RULES rule — fix it in the same pass. CORE RULES is always the authority; working-surface files follow. No questions, no parking. Full rule: `Rules for Claude.html § 3`.
- ⚠️ **After any CORE RULES approval — work the cascade before announcing done.** Read `Brain/Reference/Change Cascade.html`, work every ✅ step for that change type, regenerate checksums, run doc_workshop_validator. A CORE RULES change without its cascade is half-done. Full rule: `Rules for Claude.html § 3 + § 5`.
- ⚠️ **Website change = fire the cascade too — not only CORE RULES edits.** A new tab/page/feature wired into the site or `assets/toolbar.js`, or any change to the navigation structure, fires the **🔗 New page added to toolbar** / **🧭 Navigation structure changed** cascade in `Brain/Reference/Change Cascade.html` — bring `Toolbar.html`, `Navigation.html`, `Guides-Index.html` (if a guide), and `Brain.md` Part 1 back into parity in the same pass. This holds even when a tab/feature was added directly outside a build: the moment the live site has something the reference docs don't describe, that's drift — fix it, don't wait to be told. `brain_check` flags toolbar pages missing from `Brain.md` Part 1 (warn). Full rule: `Rules for Claude.html § 3` + `Change Cascade.html`.
- ⚠️ **No AskUserQuestion — ever.** The Cowork popup is never invoked for any Travel task. Start immediately. Full rule: `Rules for Claude.html § 4`.
- ⚠️ **Never fetch GitHub for rules/guides.** `dbellinello.github.io` and all GitHub URLs are off-limits unless Dani explicitly asks in the current message. All rules and guides live in Drive — `Brain/CORE RULES/` and `Travel/`.
- ⚠️ **Make the call — only CORE RULES fix/edit questions park.** Every gap Claude's tools can resolve (connector, search, fetch, live browser reads including script reads on pages that never settle, operator sites) is resolved in the same session. `❓ Open Questions` holds only questions concerning `Brain/CORE RULES/` fixes and edits. Full rule: `Rules for Claude.html § 3`.
- ⚠️ **Tour-first always.** Viator MCP → GYG → TripAdvisor before venue site. Bar: 4.5+★ · ≥6 reviews.
- ⚠️ **Zero money in shipped guides.** No `$` `€` `£` `¥` or ISO codes — ever.
- ⚠️ **Tilde has one home — `⏰`.** `~` is valid only as the `⏰` Avg Time Spent prefix (`⏰ ~1 hr`). Never on `⏳` Duration (exact: `⏳ 45 min`), never on walk/ride/start/travel-time, never in prose. One carve-out, no others. Full rule: `Icon Order and Format.html` § 2.
- ⚠️ **No fabrication.** Every fact live-verified this build. Memory from past builds is not a source.
- ⚠️ **No placeholders.** `{TBD}` / `{TODO}` / "fill in later" = fabrication.
- ⚠️ **Every link live-verified — every time.** Including every edit inside a session.
- ⚠️ **Photos in same pass as stop research.** Never deferred.
- ⚠️ **Wide wins over detail (stop photos).** Facade > interior. Wide > close-up.
- ⚠️ **Icon format — read the canonical file.** `Icon Order and Format.html` before any stop box.
- ⚠️ **Validator before "done" — every scope.** Any session touching guide HTML runs validator to 0 failures. Scope of change is not an exemption.
- ⚠️ **Validator check before any guide fix.** Write the check first; fix the guide after.
- ⚠️ **Skip list ≠ guide rebuild — but every rebuild rechecks the skip list.** Editing the Cities Skip List (`Brain.md` Part 4) changes no guide on its own and never triggers a rebuild — just update Brain.md and stop (it's Drive-only, no push). The "Skipping:" footnote refreshes only when the guide is itself rebuilt with new content. Whenever you DO rebuild a guide with new content, recheck that city's skip list and bring its footnote into parity — add newly-listed venues, drop removed ones, keep the order. Enforced: `validate_itinerary.py` hard-fails when a city with a `## City` skip-list entry has a footnote whose venue set ≠ the current list (no missing, no extras). Rule home: `Skip List - Extra Section.html § 5`. (added 2026-06-21)
- ⚠️ **Don't read past guides — they breed drift.** `Guides/` is output, not reference or format template. Old guides are frozen point-in-time builds, valid only under the brain that shipped them — so they are stale-by-design the moment a rule changes. Opening one as a template imports its drift into the new guide: one stale guide becomes many. Format comes from `Brain/CORE RULES/`, content from live research. Only open a guide file when editing that exact guide or wiring the index/chain/map pins (the named carve-outs). Full rule: `Rules for Claude.html § 6` (Don't read past guides).
- ⚠️ **No hedging in factual rows.** "typically open" / "usually takes" / "approximately" = banned. Look it up or omit.
- ⚠️ **🚕 ride time = mapping-service driving mode** (Google or Apple Maps; a local mapping service where neither covers the region — Motion Rule § 1). No ride-share APIs, no estimators.
- ⚠️ **One archive — `Travel/archive/` only.** Never create a subfolder archive anywhere else.
- ⚠️ **Editing ≠ disposing.** Editing an existing file in place never triggers a delete/archive step. Disposal fires only when a new versioned file replaces an old one (e.g. `paris_v8.html` replacing `paris_v7.html`) — the old **tracked** version is deleted (git preserves it), an untracked old file moves to `Travel/archive/`. Self-diagnostic: if the next action after disposing of a file is editing a file with the same name — stop. Just edit the original. Full rule: `Rules for Claude.html § 6`.
- ⚠️ **Build artifacts belong in `_build/` directories, not at Travel root.** Every guide has a `Travel-Website/Guides/{City}/_build/` folder. All build logs, verification logs, old versions, and temporary build files for that guide go there: `ship.log`, `build_state.md`, `verification_log.json`, and prior `_v1.html` / `_v2.html` versions of that guide. Non-guide build artifacts (global ship outputs, whole-fleet audits, utility scripts) go to `Brain/scripts/` if they're reusable tools, or `Travel/archive/` if they're one-off outputs or old versions. The Travel root stays clean: only reference files (shopping profile, deployment protocol), Drive-only content (CLAUDE.md, GUIDE_DEPLOYMENT_PROTOCOL_v1.md), and tracked config (CLAUDE.md) live there. Full rules: `Brain/Reference/Travel-Folder-Organization.md`. (added 2026-06-28)
- ⚠️ **Edit in place — NEVER create a new file to change an existing one.** To change any existing Drive file, use the `Edit`/`Write` file tools on that file's exact mounted path. NEVER use the Google Drive connector's `create_file`/upload to modify a file that already exists — it produces a duplicate named `Name (1).ext` and the real edit is lost. NEVER write updated content to an `outputs/`/scratchpad temp file and pull it in via an include (`<?python open('/sessions/.../mnt/outputs/…') ?>`) — the temp dies with the session and strands the content. Self-diagnostic: a `Name (1).ext` sibling appeared, or a file's byte size didn't change after an edit = the in-place edit failed. Archive the `(1)` stub to `Travel/archive/`, then redo the edit directly on the original path. The Drive folder is mounted every session; the mounted path is always the write target.
- ⚠️ **"Updated" stamp = content currency, set it by hand on a content refresh.** Every city guide shows "Updated Month Year" at the TOP (right-aligned under the title block), injected by `toolbar.js` from the EXPLICIT `data-updated="YYYY-MM"` attribute on the guide's toolbar-mount div — NOT `document.lastModified` (changed 2026-06-21; lastModified bumped on every file touch / deploy, so all guides read the deploy month — the opposite of content currency). When you do a genuine content refresh (add/swap/rewrite a stop, restaurant, hotel, or substantive info) bump `data-updated` to the current `YYYY-MM` in the same edit. Do NOT bump it for bug fixes, typos, cosmetic touches, validation re-stamps, or format/CSS/toolbar changes. TB-11 hard-fails a guide whose toolbar-mount lacks a well-formed `data-updated`. Full spec: `Brain/Reference/Toolbar.html § 10`.
- ⚠️ **Active/selected/hover-fill states — terracotta only, never gold.** When writing any filter pill, region selector, nav tab, or button that highlights on activation, the background color MUST be `#b85c2a` or the terracotta gradient (`linear-gradient(135deg,#7a3b1e 0%,#b85c2a 55%,#d4874a 100%)`). NEVER use `#8a6c1a` or `var(--accent)` as a filled background on `.active` / `.on` / `.selected` / `:hover` / `.copied` states. Gold (`--accent`) is for text color and borders only. Full rule: `Brain/Reference/Colors and Font Size.html` § 11. Enforced: `brain_check.py check_active_pill_color` (hard-FAIL at session start). (added 2026-07-07)
- ⚠️ **Pill/button REST state is WHITE, never beige — `#fdf8f0` is a card token, not a pill token.** The unselected state of every boxed link/button pill is `background:#ffffff` (or `var(--surface)`) + gold `#c8a44a` border + `6px` radius — the canonical `.also-on-this-site-pill` look. NEVER fill a pill/button with a cream/beige background: `#fdf3e0` (and siblings `#fdf6ec`/`#fef3e0`/`#fdf4e3`/…) are pure drift and have no legit background use; `#fdf8f0` (`--c-warm-bg`/`--warm`) is the *shared warm card/box/banner* background and must never land on a pill/button. Pills are white; cards are warm; never the reverse. This keeps recurring (European-Train footer buttons shipped `#fdf3e0`, fixed 2026-07-13). Full rule: `Brain/Reference/Pill-Standard.md` § "Rest state = WHITE". Enforced: `brain_check.py check_no_beige_pill_button` (hard-FAIL at session start + pre-push). (added 2026-07-13)
- ⚠️ **`guide-style.css` is the single CSS source of truth.** Edit `Travel-Website/assets/guide-style.css` directly — no sync step needed. Every guide HTML loads `../../assets/guide-style.css`. (`Brain/Reference/Guide Style.css` has been retired and archived.)
- ⚠️ **Guides-Index topbar layout — LOCKED (2026-07-08).** `#search-wrap` is full-width, search bar (`#guide-search`, 360 px) stays centered inside it. `#status-toggle` (All/Been/Want) is `position:absolute; right:calc(50% + 300px)` inside `#search-wrap` — placing it just left of the search bar, clear of the pill row. Do NOT move it to `left:0` (goes to page edge), do NOT put it back in the topbar flow (eats pill-row space and hides "Best of"), do NOT change `right` without visual check. On mobile ≤600 px: `#status-toggle` resets to `position:static; width:100%` and stacks above the input. Full spec: `Brain/Reference/Navigation.html` § 8.
- ⚠️ **Mobile baseline — every shareable page carries it.** Any new or edited user-facing page (site home `index.html`, guides index, Trip Essentials, Maps, On The Go) needs the `<meta name="viewport">` tag and a link to `Travel-Website/assets/mobile.css` (the universal defensive baseline — kills horizontal overflow, caps tables/images, sane tap targets). Don't hand-roll per-page `@media` blocks. Run `python3 Brain/scripts/mobile_check.py --apply` — it injects both at the correct relative depth, idempotently. Surfaced at session start (Step 1 — `guide_tools.py start`) and hard-gated at ship (`--strict`). Edit the baseline once in `Travel-Website/assets/mobile.css`; every page updates. Guides themselves are covered by the defensive block in `guide-style.css` — never edited per-guide (LOCKED 2026-06-12: new guides link `guide-style.css` only, not `mobile.css`).
- ⚠️ **New main page = add to the Main Pages index, same pass.** Any page added to the toolbar `ITEMS` array in `Travel-Website/assets/toolbar.js` must also be added to `Travel-Website/Website-Main-Pages-Links.html` — the Main Pages hub shared with friends — as a leaf with its live link. (`Travel-Website/index.html` is a redirect stub → `Guides-Index.html` — not the Main Pages directory.) Toolbar and the Main Pages index must list the same set (Guides index is the one deliberate omission). **Now enforced:** `brain_check` `check_toolbar_in_main_pages` warns on any toolbar page missing from the hub (parity), and `check_main_pages_internal_links` warns on any broken relative `.html` link across the Trip-Essentials + site-root pages (cross-link rot — wrong depth, stale version filename, renamed folder, link to an unbuilt city). Both surface at session start; fix in the same pass. Full rule: `Brain/Reference/Toolbar.html` § 4 (added 2026-06-21).
- ⚠️ **Static mobile checks aren't enough — RENDER-check shareable pages.** `brain_check` (§14 search colors) and `mobile_check` (viewport + mobile.css present) are STATIC and pass even when a page draws wrong at phone width. The build crib keeps reshipping the same mobile regressions (filter pills rendering skinny/square — 11-24px instead of the 31px/13px/6px standard — and horizontal overflow) because nothing measured the rendered result. After adding or editing ANY shareable page (Trip-Essentials, guides index, home), run `python3 Brain/scripts/validate_mobile_render.py [<file>]` — it loads each page at 393px and flags off-standard filter/nav pills + overflow. Runs warn-only at session start (`guide_tools.py start` Step 3b). **Systemic fix for skinny pills:** the canonical pill rule lives in `Travel-Website/assets/mobile.css` (`.pill, .filter-btn, .filter-pill, .tier-pill, .fchip, .stab, .stats-nav a` → 13px/8px14px/6px `!important`). New pill class names go THERE, once — never a per-page `@media` shrink that fights it. (added 2026-06-20)
- ⚠️ **Mobile look is LOCKED as the default — two chip forms, don't drift.** The 2026-07 mobile redesign is the site's new default and is centralized in `Travel-Website/assets/mobile.css` + `assets/guides-index-style.css`. Mobile filter/nav chips have **two approved forms**: **Form A** — compact rounded pill (`13px / 8px 14px / 6px radius`) on short pill rows (Before You Go, SIM, Delta, Visas…); **Form B** — full-width glued-grid tile (`46px / 12px / 0 radius`, in a `repeat(3,1fr)`/`gap:0` grid) on full filter PANELS + section-nav grids (guides-index `.fchip`/`.tchip`/`.lchip`, `.stats-nav`/`.jump-nav`, European Train `.controls .filter-btn`). Form B is deliberate — **not** a skinny-pill regression (that one is SHORT, ~11px). Both are specced in `Colors and Font Size.html` § 17 + `Pill-Standard.md`. **Anti-drift gate:** `brain_check.py check_mobile_design_system` HARD-FAILS at session start if the shared CSS loses any of 13 locked invariants (no-overflow guards, banner 14px, Form A pill, Form B grid+tile, 48px search, toolbar gradient, the three index panels). A DELIBERATE change updates the invariant in `brain_check` **and** § 17 in the same pass; an accidental edit/revert fails the gate. `check_pill_rest_state` is DESKTOP-only (strips `@media`) and no longer scans `.stab` (a control button). (added 2026-07-11)
- ⚠️ **Mobile guide-page + guides-index format (2026-07-11) — enforced in shared CSS/JS, don't revert from a stale doc.** The mobile **guide banner** is ONE tappable white card glued directly under the gradient line: hotel name on top (14px / 600 / gold `#8a6c1a`), address the single-line Google-Maps link with a `↗`; `.title-address a{min-height:0}` cancels mobile.css's 40px `a{}` tap-target that was inflating it. `toolbar.js` runs three mobile-only movers: `repositionUpdatedStamp()` (the "Updated" stamp drops to the bottom-right of "Also on this site"), `repositionReadAbout()` (READ ABOUT to the bottom of Trip Overview), and `shortenTrainPill()` (the resource pill DISPLAYS "European Train" — the HTML keeps canonical "European Train Guide" so `check_also_on_site_pill_labels` + TR-4/TR-6 pass; never edit the guides' HTML for this). **"Also on this site"** is a compact 3-row grid (`repeat(6,1fr)`: 6 short pills span 2 cols = 3-across, Stats · European Train span 3 cols = 2-across; non-EU Stats `[href*="Stats"]:last-child` spans full width), all 12px single-line. **Guides-index topbar (mobile):** the two stat lines fold onto one row, the All/Been/Want toggle sits BELOW the search (`#status-toggle order:2`), 34px / 12px gold `#8a6c1a` matching the filter pills, and **starts cleared** (no `on` on load; the status filter defaults to `'all'` independently), highlighting terracotta+white on tap and clearing on re-tap. All of this lives in `guide-style.css` / `mobile.css` / `toolbar.js` / `guides-index-style.css` (single source of truth) — desktop is unchanged. Full spec: `Hotel Banner.html` § 2b · `Colors and Font Size.html` § 17 · `Navigation.html` § 8 · `Toolbar.html` § 10 · memory `mobile_ui_format_2026_07`. (added 2026-07-11)
- ⚠️ **Title banner + page gutter are LOCKED — don't invent a format.** Every non-guide page's title banner is identical: a single text-only `<h1>` (NO emoji — it goes in the toolbar label), **14px / weight 700 / colour `#3d3a32` / uppercase / 0.06em**, on a transparent block with a 3px terracotta gradient underline — same desktop and mobile, defined once in `assets/web-travel-style.css` + `assets/mobile.css`. The page gutter is locked at **0 32px desktop / 0 14px mobile** — carried by the shared `.wrap` OR a page-specific container at that same gutter (most pages use the latter; both are correct). Never put horizontal padding on `<body>`, never override `.wrap` off 32/14 (unless width-capped). Page files are **hyphenated** (`Currency-Guide.html`) — a space-named twin is a stray dup (archive it). Cribs keep drifting all of these (gold/600 titles, 18px banners, emoji `<h1>`, body gutters, the space-named Currency dup) — so `brain_check` now hard-fails each (`check_banner_title_size` size+colour+weight incl. the shared sheets · `check_banner_h1_text_only` · `check_page_margins` · `check_page_filename_spaces`), all glob-driven so a **brand-new page is gated at session start** (`guide_tools.py start` runs `brain_check` first and must exit 0). Building a new Essentials page: copy a compliant page's banner + container shell, wire toolbar+hub, then run `brain_check` to 0 — never design a new title or margin. Full spec: `Brain/Reference/Colors and Font Size.html` § 16 + `Trip-Essentials/Essentials-Pages-Rules.md`. (added 2026-06-21)
- ⚠️ **Guide photos must stay ≤ 1MB / ≤ 1600px — no full-res originals.** Guides serve photos from `_build/assets/`; Wikimedia "originals" are often 4000-5000px / 5-8MB despite a `800px-` filename → heavy mobile load and, in bulk, a threat to the 1GB Pages limit. `commons_photo.py` should save the ~800px thumb, but oversized files still slip in. **The ship gate auto-downscales the shipping guide's photos** (`guide_tools.py ship` → `validate_image_sizes.py --fix <guide_dir>`, best-effort). Check/fix anytime: `python3 Brain/scripts/validate_image_sizes.py` (report) / `--fix` (resize to 1400px @ q80) / `<guide_dir>` to scope. Photo-PNGs can't shrink by resize alone — convert those to JPEG and update the HTML `src`. (Standard set 2026-06-20 after multi-MB photos were found across shipped guides.)
- ⚠️ **Repo publishes ONLY `Travel-Website/` — and `_build/assets/` IS served (guide photos).** GitHub Pages republishes the **entire** repo tree on every build and enforces a **~1 GB total-site limit** — splitting into smaller pushes does NOT help, each build publishes the whole site. The published repo is slimmed to `Travel-Website/` + `.nojekyll` + `.gitignore` (whitelist `.gitignore`: `/*` then re-include those). Everything else (Icons Library ~672 MB, Brain rules+scripts, archive, Hotel Research, shopping profile, scratch folders) is Drive-only authoring material — never published; the cribs read it from the mounted Drive, not git. **CRITICAL CARVE-OUT:** guide HTML serves its photos from `src="_build/assets/800px-*.jpg"`, so **`_build/assets/` is published site content and MUST stay tracked.** `.gitignore` ignores `_build/*` but re-includes `!**/_build/assets/`. The rest of `_build/` (verification logs, build_state, raw originals) stays out. Do NOT blanket-untrack `_build/` — that 404s every guide photo (happened 2026-06-20, commit b574f516 → fixed in 67022950). Symptom of over-1GB: every deploy errors instantly with `"Page build failed"` / `duration 0` (looks like an infra flake but is NOT transient). Check size: `git ls-tree -r -l origin/main | awk '{s+=$4} END{print s/1048576" MB"}'` — keep well under 1024. **STANDING DISCIPLINE — every crib, every session:** the site once crossed the 1 GB cap and deploys hard-failed (`Page build failed` / duration 0); the repo was then **deliberately slimmed and the folder cleaned** — published tree cut to the `Travel-Website/`-only whitelist, and bulk/cruft pulled out of the tracked tree (Icons Library ~672 MB, Brain rules+scripts, archive, Hotel Research, scratch folders, stale git worktrees, FUSE `.fuse_hidden*` / `.DS_Store` artifacts). **These removals are intentional and permanent — done because we passed 1 GB.** Do NOT treat a file that's missing from git as lost: if it's authoring material it's Drive-only **by design** (read it from the mounted Drive). Do NOT re-add bulk to the published tree, re-track `Brain/` / `Icons Library/` / `archive/`, or blanket-track `_build/`. Before any large add, re-check the size and keep the published tree lean. `brain_check`'s `check_no_deploy_breakers` blocks committed symlinks/`.fuse_hidden`/`.DS_Store`; the total size stays every crib's responsibility. Full record: memory `project-pages-1gb-limit` + `Brain/Reference/audit_log.md` 2026-06-21.
- ⚠️ **Shared assets live in `Travel-Website/assets/` — permanent.** `assets/toolbar.js` + `assets/weather.js` + `assets/guide-style.css` + `assets/mobile.css` + `assets/climate.json` (moved out of Guides/ in the 2026-06-13 reorg; `footnote.js` removed 2026-06-21). Every page loads them from `assets/` at its own depth below the site root: site home `index.html` → `assets/toolbar.js`; depth-1 pages (guides_index, Trip Essentials) → `../assets/toolbar.js`; depth-2 pages (Guides/{City}, Trip Essentials/Maps, Plug Adapter) → `../../assets/toolbar.js`. brain_check fails on a stray copy at the Travel root or in Guides/. Full rule: `Brain/Reference/Toolbar.html` § 1–2.
- ⚠️ **Guides-Index.html — update on every ship, same pass.** Six steps: new card (with `data-status="want"`) + **three inline-data entries** for the card (`CLIMATE_INLINE` 12-value hi/lo arrays · `COST_DATA` {tier,currency} · `SAFETY_DATA` {level,note}) + predecessor/successor data-guide-next/prev + counts + toolbar data-prev/data-next + map pin (see next rule). Also add city to `Brain/Reference/Status Dots — guides_index.md` as `[ ]` in the same pass. **Ship gates enforced:** `guide_tools.py ship` hard-fails if the city is absent from Status Dots — guides_index.md (`_check_guide_in_status_dots`, added 2026-06-15) OR if the card is missing any of its three inline-data entries (`_check_guide_inline`, added 2026-06-21 — the card existing is no longer enough; its on-card filters must have data). Full rule: `Brain/Reference/Ship Checklist.html` § 11.
- ⚠️ **Flight-time view (guides_index "By flight time from Seattle") — validate after any FMAP or colour edit.** The `FMAP` data block carries each guide's stops + hub + time; its schema, the colour scheme, and the routing authority are documented in a comment right above `var FMAP`. Routing (stops + hub) must match `Trip-Essentials/Delta-Routes-SEA.html` tiers; the dot (CSS `.fdot`), card border (JS `COL`), and Stops chip emoji must all be one colour family per routing. Adding a guide that ships needs a matching FMAP entry — **ship gate enforced (added 2026-06-15):** `guide_tools.py ship` hard-fails if the city has no FMAP entry (`_check_guide_fmap`). Run `python3 Brain/scripts/validate_flight_index.py` — must exit 0. Full check list: `Brain/Reference/Validator Index.html` § flight-time view.
- ⚠️ **Map pins — add on every ship, same pass.** Add the pin to the **one unified world map** `Travel-Website/Trip-Essentials/Maps/World-Map.html` (the 7 region maps were consolidated into a single fly-to-region world map on 2026-06-20; they're now redirect stubs into it). Add `['CityName', lon, lat, '../../Guides/City/file.html']` to the single `PINS` array, under the matching region comment block (the map sits at depth-2, so the link climbs two levels to `Guides/`). Ship gate scans `World-Map.html` and blocks if the pin is missing.
- ⚠️ **Climate/Weather data — add the city on every ship, same pass.** Both `🌤️ Weather` toolbar tabs — **By Climate** (`Climate-Finder.html`) and **By City** (`Trip-Essentials/Weather.html`) — read `window.TravelClimate`, baked into `assets/weather.js` from `assets/climate.json`. A guide that ships without climate normals is invisible in BOTH tabs. After the map pin is added, run `python3 Brain/scripts/build_climate.py` (it reads the map pins to know which cities to fetch), then `python3 Brain/scripts/validate_climate_coverage.py` (exit 0). **Ship gate enforced (added 2026-06-15):** `guide_tools.py ship` hard-fails if the city is absent from `climate.json` or the `weather.js` baked block (`_check_guide_in_climate`).
- ⚠️ **Stats · Safety · Currency — add the city on every ship, same pass (ship gates added 2026-06-15).** When a guide finishes it must also land in three more pages, all enforced by `guide_tools.py ship` (hard-fail): **📊 Stats** (`Travel-Stats.html` — auto-generated; the gate rebuilds via `build_travel_stats.py` then hard-validates with `validate_travel_stats.py`), **🛡️ Safety** (`Safety-Guide.html` — **auto-generated as of 2026-07-06** from `Brain/scripts/safety_levels.json` by `build_safety_guide.py`, which `guide_tools.py ship` runs automatically on every ship; add/edit the city's entry — level (L1–L4), file, data_city, display, country, verified — in `safety_levels.json`, never hand-edit `Safety-Guide.html` directly since it is overwritten on the next ship — `_check_guide_in_safety` hard-blocks ship if the shipping city is absent from `safety_levels.json`, and `validate_safety_guide.py` checks the rendered output stays in sync with `guides_index`), and **💰 Currency** (`Currency-Guide.html` — **per-country**; only if the guide adds a **new country**, add it to the `COUNTRIES` list in `Brain/scripts/build_currency.py` and run `build_currency.py` — `_check_guide_in_currency` blocks if the shipping city maps to no country). All four data-page gates (climate, safety, currency, stats) run in the same post-ship pass as the index card, map pin, status dots, and FMAP.
- ⚠️ **Coverage gates Steps 7–13 — enforced in both `update-index` and `ship` (added 2026-06-22).** Every guide must land in seven more surfaces before ship passes: **Step 7** — US cities in every category of `Stats-Across-US.html`. **Step 8** — non-US city's country in the correct regional stats page (Europe-Stats/Asia-Stats/Caribbean-Stats/South-America-Stats/Stats-Across-Canada). **Step 9** — `Time-Zones.html` CITIES array entry with `name/tz/utc/url`; if folder ≠ city name add `guide:'Folder'`. **Step 10** — `Delta-Routes-SEA.html` card with `data-guide="Folder"` (research SEA routing on Google Flights/delta.com; Seattle exempt). **Step 11** — guide HTML must carry `<!-- transit-card: Card Name | Network -->` or `<!-- transit-card: none -->` (research "[City] transit card" during build; run `guide_tools.py no-transit-card <City>` to auto-write the "none" comment + log to ship_log.md; if city has a card also add its `transit-row` div to `City-Transit-Cards.html`). **Step 12** — new country: row in `Visas.html` (Caribbean grouped under "Caribbean" row; US exempt). **Step 13** — new country: entry in `Tipping-Guide.html` COUNTRIES array. All 13 steps fire inside `update-index` and `ship`.
- ⚠️ **Push site fixes freely — but any new or modified HTML must pass validation before it can push.** GitHub Pages republishes the whole `Travel-Website/` tree on every push, so a broken page goes live the moment it's pushed. A **pre-push git hook** enforces two gates: (1) **Guides** — any guide HTML ADDED/MODIFIED in the push must carry a valid SIGNED `<!-- validation: passed -->` stamp (written by `guide_tools.py ship/validate`; hand-typed/bulk stamps and edits after validation both block); (2) **Non-guide pages** (Trip-Essentials, Best-of, site root, Maps) — any HTML ADDED/MODIFIED triggers `brain_check.py`; push is blocked if brain_check exits non-zero (banner size, pill colour, margins, active-state colour, CSS all enforced). Pure asset pushes (CSS/toolbar.js/climate.json) pass through. In-progress guides stay Drive-only (untracked) until they ship — the moment a guide is tracked it must be validated. To unblock: `git rm --cached <guide.html>` + its `_build/` siblings, or finish via `guide_tools.py ship`. The hook lives at `Brain/scripts/git-hooks/pre-push` (tracked) + `Brain/scripts/pre_push_guard.py`; it's activated repo-wide via `core.hooksPath`, which `guide_tools.py start` sets/verifies every session (Step 0). Check the tree anytime: `python3 Brain/scripts/pre_push_guard.py` (exit 0 = safe to push). **Validation is LOCAL; the deploy just publishes (reimplemented 2026-06-27):** the stamp is content-bound + HMAC-SIGNED (`Brain/scripts/validation_stamp.py`) — `validate_itinerary.py` writes it on a clean pass. The deploy (`deploy-pages.yml`) does NOT validate — it stages `Travel-Website/` + `.nojekyll` and publishes. Full record: memory `project_validation_gate_signed_stamp`. Pages deploys via **GitHub Actions** (`build_type: workflow`); URLs unchanged (`the-voyager-expert.github.io/Travel/Travel-Website/…`). Rollback: set Pages build type back to `legacy`. (added 2026-06-21; non-guide gate added 2026-07-07)
- ⚠️ **Serialized publish — cribs build isolated, one publisher does all shared writes (added 2026-07-05).** The concurrency-safety model that ended "cribs undo each other." A crib builds + validates a guide in ISOLATION (its own `Guides/<City>/` folder), then `ship` hands it to the SINGLE serialized publisher: `guide_tools.py ship` calls `publish_queue.py --enqueue <guide>` then `--drain`. The publisher runs under the push lock and does ALL shared-surface work — reverts the 3 root surfaces (Guides-Index / World-Map / Time-Zones) to HEAD (dropping every crib's uncommitted leaks so a neighbour's half-built card never goes live), merges, re-applies ONLY the queued cities idempotently (`guide_surfaces.py` insert-if-absent appliers, proven on 205 cards + 820 data entries), runs the derived builders, scoped-commits + pushes. **Convergent by construction:** any number of cribs, any order → correct union, no leak, no lost card. **Never bypass it:** don't hand-`git commit`/`git push` a guide or hand-edit shared pages to "publish" — always `guide_tools.py ship`. **Per-guide lease:** `init` claims `Guides/<City>/_build/.lease`, `ship` releases; before touching ANY guide you did not build, check `python3 Brain/scripts/crib_safety.py owned-by-other <Guides/City>` — if it prints True, hands off (another crib is building it). **Atomic writes** (`crib_safety.atomic_write`, used by the publisher + all builders) mean no reader ever sees a half-written page. ⚠️ The first `drain` reverts the 3 root surfaces to HEAD — settle a dirty working tree before the first real ship. Full record: memory `project_serialized_publisher`.
- ⚠️ **Punt-detection.** "I can't access Viator" / "ratings need manual lookup" = wrong. Run the tool. Chrome MCP bypasses bot-blocks.
- ⚠️ **"Also on this site" — exact canonical set, exact order, ALL required:** 🌤️ Weather · 🕐 Time Zones · 🔌 Plug Adapter · 💰 Currency · 🛡️ Safety Guide · 🪪 Visas · 📊 Stats (region-appropriate) · 🚆 European Train Guide (EU guides only, always last). All seven base pills are **mandatory on every guide** (absence fails, not just wrong order); the EU Train pill is the only region-gated one. Stats label is the live toolbar form — `Stats Across the Caribbean` / `Stats Across South America` (NOT "Caribbean Stats" / "Stats · South America"). Pill class is `also-on-this-site-pill` (the retired `trip-resource-pill` is unstyled and forbidden). Nothing else ships here — 🚇 City Transit Cards · 📱 SIM Cards Abroad · 🧾 Tipping Guide · 🛋️ Airport Lounges · 🌤️ Climate Finder are all banned and hard-fail. Enforced by `brain_check.py check_also_on_site_pill_labels` (icon+name lock, session start) + `validate_itinerary.py` TR-2/TR-4/TR-6 (class · order · full required set, ship gate) + `validate_also_on_site_pills.py` (standalone backstop). **Watch the silent no-op:** a validator matching the retired `trip-resource-pill` class finds zero pills and passes everything — that bug hid broken also-blocks for ~all guides until 2026-06-27.
- ⚠️ **No full EoI cards in Trip Overview day-card area.** Days-only grid. Compact nav pills permitted.
- ⚠️ **Day count in prompt = trip not in Trips.html.** "Amsterdam" → look in Trips.html. "Amsterdam, 4 days" → skip lookup, run hotel research.
- ⚠️ **Calendar hotel blocks — end = checkout + 1 day.** Google Calendar all-day events use exclusive end dates.
- ⚠️ **Resuming a build — read `build_state.md` first.** If Phase 6 unchecked, the guide is not done.
- ⚠️ **Never archive an incomplete guide build.** Any guide folder with a `_build/build_state.md` (Phase 6 unchecked, or no HTML yet) is a resumable build — NEVER archive, remove, delete, or "clean it up," even if it looks empty or stalled. A stalled crib is finished by another crib from `build_state.md`. There is no step that gets rid of incomplete guides. `guide_tools.py start` flags them as "maybe in progress — check later" — that flag is the only action. Archive one only on Dani's explicit per-case say-so. Full rule: `Rules for Claude.html § 2` (What does NOT trigger auto-archiving).
- ⚠️ **Every guide ships a Claude Inspiration section — mandatory, no exceptions (added 2026-07-12).** `Brain/CORE RULES/Claude Inspiration - Extra Section.html` § 1 changed from "Optional — ships only when something genuinely worth sharing surfaced" to "Required — every guide ships one." Research always turns up something genuinely worth sharing; find it and write it before ship. No negative-finding line and no placeholder stand in for it — that was the one carve-out this section used to have, and it's retired. Build it in Phase 5, same pass as the extra sections, always last before the Skip List footnote. Icon, title, and color theme stay Claude's free pick per guide (`Guide Structure.html` § 2, `Claude Inspiration - Extra Section.html` § 2) — only the *presence* of the section became mandatory, not its content shape. **Hard-gated:** `validate_itinerary.py` fails a guide missing the `claude-inspiration` div (checked independently of the section's other format checks, so absence can't hide behind them); the Trip Overview ✨ Claude Inspiration pill is now also required (Overview extras sync check A). **Backfill:** a fleet scan on 2026-07-12 found 60 of 219 shipped guides predate this rule and currently lack the section (Alesund, Amsterdam, Bologna, Cairo, Lisbon, London, Paris, Singapore, Venice, Washington-DC, and 50 others — full list in that session's transcript) — not auto-fixed; each is forced to acquire the section the next time it is rebuilt or edited and re-validated.
- ⚠️ **Every guide ships with a Read About page — mandatory, no exceptions (added 2026-07-10).** A guide is not done until `Travel-Website/Guides/{City}/{slug}-read-about.html` exists beside it (`{slug}` = the guide filename minus `_vN.html`, so `lisbon_v4.html` → `lisbon-read-about.html`). It is long-form editorial prose on the city's history, culture, and context — prose only, no stop boxes, no prices, no fabrication. It must be linked **both ways**: the guide injects the `READ ABOUT {CITY}` link into the Trip Overview title bar, and the read-about page's `.story-back` + `.story-footer-back` links name the guide's **current** `_vN` filename (a version bump orphans a stale back link). Reference implementation: `Travel-Website/Guides/Lisbon/lisbon-read-about.html`. Build it in Phase 5, in the same pass as the extra sections — never deferred. **Hard-gated in three places:** `validate_itinerary.py` fails without it (→ no signed stamp → `pre_push_guard.py` blocks the push, so it is impossible to publish a guide without one), `guide_tools.py ship` blocks (`_check_guide_story_page`), and `update-index` Step 14 blocks. `validate_guide_coverage.py` reports the fleet-wide row. Full spec: `Brain/Reference/Read-About-Pages.html`. **Backfill:** 143 of 208 guides predate this rule — each is forced to acquire a read-about page the next time it is validated; until the queue empties the coverage row is warn-tier (`BACKFILL_SURFACES`), so it does not red-line session start or the Pages deploy.
- ⚠️ **Index card subtitle (`dest-sub`) — gated, never autonomous.** A card's italic sub-line surfaces a *non-obvious natural marquee* a guide covers — caves, lakes, canyons, waterfalls, geysers, fjords, parks (e.g. Ljubljana → "incl. Postojna Cave · Lake Bled"). Especially valuable when the base-city name is unknown/not famous — you'd never guess from the index that an obscure city is the gateway to a major natural feature (the access town for the Grand Canyon, etc.). NEVER cathedrals, museums, palaces, or obvious city sights (= drift). A crib never adds one on its own judgement — it parks a proposal in `To Do List/To_Do_List.md` § 🪧 Index Subtitle Proposals and adds it only after owner approves. Full rule: `Brain/Reference/Ship Checklist.html` § 11.
- ⚠️ **No new files in `Brain/Reference/` without explicit permission.** Claude edits the existing 25+ files there freely but never creates a new file in `Brain/Reference/` without explicit per-session approval. See `Brain/Reference/Brain.md` Part 1 for the full file list.
- ⚠️ **Absence of rule = don't ship.** No rule authorizes it → it doesn't ship.
- ⚠️ **Agent prompts are mini-rules.** Every rule the agent's output must respect goes in the prompt.
- ⚠️ **SHARED CSS ONLY — LOCKED RULE (added 2026-06-23).** All reusable styles must live in shared CSS files, never duplicated across pages. (a) **Color rule:** NEVER hardcode hex colors in pages — use CSS variables: `var(--bg)`, `var(--text)`, `var(--surface)`, `var(--accent)`, `var(--border)`, `var(--border2)`, `var(--muted)`, `var(--warm)`, `var(--hover)`, `var(--navy)`, `var(--green)`, `var(--gold)`. (b) **Base style rule:** NEVER define `body {}`, `* {}`, `:root {}` in pages — these belong ONLY in `web-travel-style.css`. Pages must link to shared CSS and inherit all base styles. (c) **Component rule:** Reusable components (classes used by 2+ pages) belong in shared CSS (`web-travel-style.css` or `mobile.css`), not in individual pages. Examples: `.pill`, `.card`, `.search-box`, `.nav`, `.stats-nav`, `.controls`. (d) **Enforcement:** Run `python3 Brain/scripts/check_css_duplication.py` before every commit. Exit 0 = no duplication. Exit 1 = violations found (move to shared CSS). **Ship gate:** `guide_tools.py ship` includes CSS duplication check (soft-warn, not hard-block). This rule enforces single source of truth for all styles — change a color once in `web-travel-style.css`, updates everywhere. Full record: `Brain/Reference/CSS-Consolidation-Final.md` (consolidated 629 colors + 60+ components in 2026-06-23 session).
- ⚠️ **Local paths use hyphens — a folder, file, or link path never carries a space (LOCKED 2026-07-05; superseded the 2026-06-27 %20-encoding rule).** A folder or file a guide/page links to is named with hyphens standing in for spaces, and every internal link to it carries that hyphenated path: `href="./Glacier-National-Park/guide_v2.html"`, never `./Glacier National Park/...` and never `./Glacier%20National%20Park/...`. Display names, titles, and map-pin labels keep their spaces; the path does not. Applies ONLY to **internal** relative links to local `.html`/`.pdf` files — external `?query=` URLs (Google Maps place searches) keep their raw spaces and are exempt. **Hard-gated, no grandfathering:** `brain_check` `check_internal_link_space_encoding` fails on any internal local-link path containing a space **or** `%20` at session start + ship. A spaced folder that has not yet been renamed to hyphens fails here until it is migrated — folder rename + relink of every reference, done **by hand** (not automated; the retired `encode_link_spaces.py --fix` that inserted `%20` no longer applies). The 45 legacy spaced guide folders (Hoi An, San Sebastian, Lake Como, …) fail this check by design until that migration is run. (hyphen rule 2026-07-05)

---

## On-demand documents

Run only when explicitly asked. Output goes to `Travel-Website/Trip-Essentials/Trips.html` — never auto-ships in a guide.

- `Travel/On Demand/Delta - On Demand.html`
- `Travel/On Demand/Hotels & Rentals - On Demand.html`
- `Travel/On Demand/Car Rentals - On Demand.html`

---

## Shopping

When asked to find, buy, or research any product — read `Travel/shopping_profile_v2.md` before responding.

---

## Two-crib architecture

Two environments, same Drive workspace:

| | Cowork (desktop) | On The Go (mobile) |
|---|---|---|
| **Root folder** | `Travel/` | `Travel/On The Go/` |
| **Capabilities** | Full read/write/edit/archive | Read + add files only |
| **Entry point** | `Travel/CLAUDE.md` | `Travel/On The Go/Rules/on_the_go_rules_v27.md` |

---

## Quick Reference

### CORE RULES HTML file index

| File | Purpose |
|------|---------|
| `Rules for Claude.html` | Master behavior doc — session ritual, authority, task execution, build discipline, parking, DriftyCat, guide content prohibitions, on-demand docs, audit, close-out, calendar, format exceptions |
| `Guide Structure.html` | Section order, extra-section sequence, cross-link anchors, build phases, build-state tracker |
| `Day Structure.html` | Day-block shape, From-Hotel opener, stop count, route shape, trip length, train day quota and pattern |
| `Trip Overview.html` | Navigation card spec, day-order rule, extras pills with canonical labels |
| `Hotel Banner.html` | Hotel banner entry — city, hotel/rental name, street address and neighborhood |
| `Stops Structure.html` | Stop selection criteria, stop types, stop box format, train day pattern, discovery/curation rules |
| `Motion Rule.html` | Walk-vs-ride threshold, motion banner formats, day opener/closer (bookends), train day motion blocks |
| `Tickets.html` | Ticket waterfall format, ticket box format (no rating floor) |
| `Photos Rules.html` | Stop-type photo selection (§3a: structure=exterior, venue=interior, else=subject), Wikimedia Commons sourcing, licenses, harvest workflow |
| `Links.html` | Link verification, subject-drift check, verification log, bot-blocked platforms, Wikipedia spec, Google Maps spec |
| `Icon Order and Format.html` | Canonical icon reference — row order, format per icon, section header icons, train icons |
| `Skip List - Extra Section.html` | Footnote for already-visited venues — ships only when city has a skip list |
| `Tours - Extra Section.html` | Tours section — source pool, rating bar, per-source minimums, entry format |
| `Getting Around - Extra Section.html` | Getting around section — ride app, tram, metro, and ferry subsections (each with what-ships criteria and entry format) |
| `Weekly Closures - Extra Section.html` | Weekly closures — recurring patterns only |
| `Local Tastes - Extra Section.html` | Local tastes — city-specific only |
| `Food Delivery - Extra Section.html` | Food delivery — platform availability per city |
| `Michelin Restaurants - Extra Section.html` | Michelin section — stars-only source, tier ordering (⭐⭐⭐ first), hotel-in-building format variant |
| `Restaurants Near Hotel - Extra Section.html` | Restaurants near hotel section |
| `Cappuccino - Extra Section.html` | Cappuccino / coffee culture section |
| `Day Trips by Train - Extra Section.html` | Day trips by train — Train/Why/Book format |
| `Shows, Performances & Concerts - Extra Section.html` | Shows section — destination-level venues only (opera, concert halls, flagship residencies), entry format |
| `Train Stations Near Hotel - Extra Section.html` | Train stations — walk times, negative-finding line |
| `Pickleball - Extra Section.html` | Pickleball section |
| `Downtown Restaurants - Extra Section.html` | Historic downtown restaurants section |
| `Claude Inspiration - Extra Section.html` | Claude's inspiration note section — required on every guide (2026-07-12) |
| `Heads Up - Extra Section.html` | City-specific heads-up notes — sourced only from Brain.md Part 3; if city absent, section does not ship |

**Operational reference:** `Brain/Reference/Cleanliness Checks.md` — 496-rule enforcer spec (how each validator catches a violation). Every entry cites its CORE RULES source. Read this when adding or debugging a validator check; use `Brain/Reference/Separation Map.md` to find the authoritative CORE RULES file for any topic.

### Validators

| Script | Job |
|--------|-----|
| `guide_tools.py ship` | Single entry point — chains validate + verify + verify-booking + index + index-inline (CLIMATE_INLINE/COST_DATA/SAFETY_DATA on the card) + pin + status-dots + FMAP + climate (both Weather tabs) + safety + currency (new country) + stats (rebuild+validate) + mobile-check |
| `mobile_check.py` | Mobile baseline (STATIC) — every shareable page (home index.html, guides index, Trip Essentials, Maps) has the viewport tag + `assets/mobile.css`. `--apply` injects both; `--strict` exits 1 on any miss (ship gate + ritual) |
| `validate_mobile_render.py` | Mobile baseline (RENDER) — loads each shareable page headless at 393px and flags off-standard filter/nav pills (envelope 26–46px h / ≥12px font / ≥4px radius; standard 31/13/6) + horizontal overflow that static checks can't see. **Form B glued tiles** (tall ≥40px AND wide grid cells) are allowed radius 0 — the deliberate mobile redesign shape — while short 0-radius skinny-square regressions still fail. `--warn` report-only; takes file args to scope. Best-effort at session start (`start` Step 3b). Needs playwright |
| `validate_image_sizes.py` | Served-photo sizes — flags guide `_build/assets/` photos over 1MB / 1600px; `--fix` downscales (ship-gate auto-heal). See DriftyCat |
| `brain_check.py` | Brain integrity — required sections, required files, ghost references |
| `validate_itinerary.py` | Guide HTML static checks — structure, toolbar, stop format, extras sections, motion rule, build-state gate (no network) |
| `verify_urls.py` | Link health — every URL returns 200 + editorial prose |
| `verify_booking_links.py` | Subject drift — h1 match for accessible links · verification log coverage for bot-blocked platforms (Viator / GYG / Michelin) |
| `commons_photo.py` | Photo resolution — Commons URLs to 800px thumbs |
| `autofix_itinerary.py` | Guide auto-repair — rewrites mis-filed booking boxes |
| `sweep_stray_travel.py` | Stray-file enforcement |
| `doc_workshop_validator.py` | CORE RULES doc integrity — run after any CORE RULES change |
| `doc_workshop_fixer.py` | CORE RULES doc auto-repair |
| `update_core_rules_checksums.py` | Regenerate checksums after CORE RULES edits |
| `audit_all_guides.py` | Full audit across all shipped guides |
| `validate_guide_coverage.py` | **Whole-fleet coverage sweep** — source of truth = the Guides/ directory. For EVERY shipped guide (HTML carries the validation-passed stamp) asserts presence, with a resolving link, in every cross-guide surface: index card + inline data + FMAP, map pin, travel stats, safety, both Weather tabs, search, status-dots, currency. The per-guide ship gates only check the one guide shipping; this backstops the rest of the fleet against later drift (a card/pin deleted in an edit, a version-bump that orphaned a link, a hand-pushed guide). Runs **warn-only at session start** (`start` Step 3c) — fleet gaps must not block work on an unrelated guide. The CI deploy gate (`.github/scripts/check_coverage.py`) is the hard enforcement layer and blocks Pages deploys on any user-facing surface gap (un-bypassable by `--no-verify`). The home/origin city (FMAP `r:"home"`) is exempt from the stats surface by design. (added 2026-06-22, made strict 2026-06-28, relaxed to warn-only 2026-07-12) |
| `validate_travel_stats.py` | Travel Stats integrity — hero counts, split bar %, bucket list, frontier chips, all guide links. Flags drift from guides_index. Exit 0 = clean; exit 1 = stale (rerun `build_travel_stats.py`). **Auto-runs on ship** (best-effort, after build_travel_stats) |
| `validate_search_index.py` | Search index coverage — every guide on disk is indexed, no ghost entries, generated date not stale (>7 days warns), **and every non-guide toolbar page is in the `pages` list** (source: `assets/toolbar.js`) with no page ghosts. Exit 0 = clean; exit 1 = gaps (rerun `build_search_index.py`). **Auto-runs advisory on ship** (whole-index audit, warn-only — won't block an unrelated guide's ship) |
| `validate_climate_coverage.py` | Climate coverage — every map-pinned city in climate.json, no error flags, weather.js baked block in sync, all monthly arrays complete. Exit 0 = clean; exit 1 = gaps (rerun `build_climate.py`). **Auto-runs advisory on ship** (whole-index audit, warn-only; the scoped hard gate is `_check_guide_in_climate`) |
| `build_safety_guide.py` | Safety Guide — regenerates `Trip-Essentials/Safety-Guide.html` from `Brain/scripts/safety_levels.json` (source of truth as of 2026-07-06). Auto-runs on every `guide_tools.py ship` (scoped hard gate `_check_guide_in_safety` blocks first if the shipping city is missing from the json). Add/edit a city's level here, not in the HTML — hand-edits to `Safety-Guide.html` are overwritten on the next ship |
| `validate_safety_guide.py` | Safety Guide coverage — every guides_index city has exactly one row, no duplicates, no stale rows, all hrefs resolve. Checks the rendered output stays in sync with `guides_index` and `safety_levels.json`; does not itself rebuild the page. Exit 0 = clean; exit 1 = drift |
| `validate_guides_index_inline.py` | Index-card inline data — every dest-card in Guides-Index.html has an entry in all three inline JS blocks (CLIMATE_INLINE / COST_DATA / SAFETY_DATA) that power the on-card weather/cost/safety filters. Whole-index audit; the scoped ship-time counterpart is `guide_tools.py ship`'s `_check_guide_inline`. Exit 0 = clean; exit 1 = a card is missing inline data |
| `build_travel_stats.py` | Travel Stats — regenerates `Trip-Essentials/Travel-Stats.html` from `Guides/Guides-Index.html` FMAP + dest-card data (been/want counts, region breakdown, flight stats, bucket list). Auto-runs as a best-effort post-step in `guide_tools.py ship`; run manually after any been/want status change |
| `build_climate.py` | Climate data — fetches Open-Meteo historical normals for every map-pinned city, regenerates `assets/climate.json` + updates baked `CLIMATE` block in `assets/weather.js` and `Trip-Essentials/Trips.html`. **Manual-only** (API calls, slow). Run after new cities are added to maps. Pre-seed `/tmp/climate_raw.json` from `climate.json` to limit to new cities only |
| `build_currency.py` | Currency Guide — fetches live USD rates, regenerates `Trip-Essentials/Currency-Guide.html` (monthly refresh). Static country→currency map lives in-script |
| `build_search_index.py` | Search index — regenerates `Travel-Website/assets/search_index.json` from all shipped guides (stops, attractions, tours, restaurants, section names) **plus a `pages` list of the Trip-Essentials / main pages** (non-guide toolbar entries — title + headings, so Visas / Currency / Stats / Lounges etc. are findable by name in the Guides-Index search box). Auto-runs as the last step of `guide_tools.py ship`; run manually after editing a guide without shipping **or after adding a page to the toolbar** |
| `build_home_stats.py` | **RETIRED 2026-07-10.** Formerly rewrote the four stat tiles on `Travel-Website/index.html` (Cities / Places mapped / Countries / Continents). That page is now a redirect stub → `Guides-Index.html`; the landing page is the guides index itself. No longer called by `guide_tools.py ship` or `publish_queue.py`. |
| `validate_currency.py` | Currency Guide integrity — country set matches the guide index exactly (no more/less), "Last update {Mon Year}" pill present and not stale. **Auto-runs advisory on ship** (whole-index audit, warn-only; the scoped hard gate is `_check_guide_in_currency`) |
| `build_theme_tags.py` | Trip-type theme tags — maintains `THEME_DATA` in `Guides-Index.html` powering the 🎯 Trip type filter (beach/wine/ski…). Holds the curated per-guide tag lists (source of truth — NOT a live re-scan); `--apply` rewrites the block, default prints counts + untagged + bad names. **Auto-runs advisory on ship** (`_refresh_theme_tags`, warn-only — untagged is acceptable, so it never blocks). Add a shipping city to the relevant lists here if it fits a trip type. Record: memory `project_theme_filter` |
| `build_lang_tags.py` | Language tags — writes `data-lang` (space-separated ISO codes) to every `dest-card` in `Guides-Index.html`, powering the 🗣 Language panel (By Language Spoken-chip filter + By Guide destination picker). Curated source of truth = `COUNTRY_LANGS` + `CITY_OVERRIDES` in-script (guide-perspective: what a traveler encounters, e.g. Curaçao = nl/en/pap); `--apply` writes, default prints count. **Auto-runs advisory on ship** (`_refresh_lang_tags`, warn-only). Add a new country to `COUNTRY_LANGS` if a shipping guide introduces one. Record: memory `project_language_feature` |
| `encode_link_spaces.py` | **Internal link-space detector** — flags relative links to local `.html`/`.pdf` files whose path carries a space (plain or `%20`). Under the hyphen rule (Links.html § 9, 2026-07-05) the fix is renaming the folder to hyphens and relinking it (a by-hand guide migration); the old `--fix` that inserted `%20` is retired. Default = report (exit 1 if any); takes file/dir args to scope. External `?query=` URLs (Google Maps) exempt. Hard gate: `brain_check` `check_internal_link_space_encoding` (added 2026-06-27; hyphen rule 2026-07-05) |
| `validation_stamp.py` | **Signed validation stamp** (shared by every gate) — `verify(html)` / `write_stamp(file)` / `compute_sig`. The stamp is `<!-- validation: passed … \| v2 \| sig=<hmac> -->`, an HMAC over the guide's whitespace-collapsed content minus the stamp itself. Defeats hand-typed/bulk stamps (no matching sig) and post-validation edits (sig no longer matches). Key: env `VOYAGER_VALIDATION_KEY` → Drive `.validation_key` → public default. CLI: `validation_stamp.py <file>` verifies (exit 0/1); `--stamp <file>` writes a signed stamp |
| `pre_push_guard.py` | **Publish guard** — blocks a `git push` that would publish a broken page. Two gates run on every push that touches HTML: (1) any **guide HTML** ADDED/MODIFIED must carry a valid **signed** stamp — missing, hand-typed/bulk, or edited-after-validation all fail; (2) **any HTML anywhere in `Travel-Website/`** ADDED/MODIFIED triggers `brain_check.py` — push blocked if brain_check exits non-zero (banner size, pill colour, margins, active-state terracotta, inline styles, toolbar standard all enforced). These rules apply to guides AND every Trip-Essentials / Best-of / site page. Pure asset pushes (CSS/toolbar.js/climate.json) pass through. Invoked by the tracked pre-push hook (`Brain/scripts/git-hooks/pre-push`, activated via `core.hooksPath`, set each session by `guide_tools.py start` Step 0). Run standalone: exit 0 = safe to push |
| `render_pdf.py` | PDF render — headless Chromium (on-demand only) |
| `validate_pdf.py` | PDF integrity (on-demand only) |
| `audit_transit_dupes.py` | From-hotel transit-time duplicate **surfacer** (advisory — never a ship gate). Groups `🚶 N · 🚕 M` from-hotel rows by identical time, flags any where one time hits ≥ N distinct 📍 pins (default 4; `--min 3` widens). Surfaces placeholder-paste suspects for manual/map confirmation — CANNOT hard-fail (text, not geography; real shared points are indistinguishable from pastes). Run on demand or in the audit pass |
| `validate_flight_index.py` | guides_index "By flight time from Seattle" view — FMAP schema, routing (stops + hub) matches `Trip-Essentials/Delta-Routes-SEA.html` tiers, and the dot / card-border / Stops-chip colours stay one consistent family per routing. Run after any edit to the FMAP or the flight-view colours. |
| `sync_tracker.py` | Travel Tracker sync — reads every `dest-card` from `Guides/Guides-Index.html` and rewrites the `DESTINATIONS` array in `Brain/Reference/Travel-Tracker.html`. Called automatically as the final step of `guide_tools.py update-index`; new cities land in the tracker with `base:"want"` the moment a guide ships. Run standalone: `python3 Brain/scripts/sync_tracker.py` (exit 0 = synced). |
| `build_days_data.py` | Syncs `DAYS_DATA` in `Guides-Index.html` from shipped guide HTML — counts days by detecting `id="dayN"` anchors. Powers the Trip Length filter. Runs on `ship`/`update-index`; the scoped hard gate is inside the Step 7–13 coverage sweep |
| `build_guide_days_coverage.py` | Regenerates `Guide-Days-Coverage.html` from `DAYS_DATA` in `Guides-Index.html` — the days-per-guide audit dashboard. Runs after `build_days_data.py` on `ship`/`update-index` |
| `validate_guide_days_coverage.py` | Checks `Guide-Days-Coverage.html` is in sync — every shipped guide appears in a tier-card city-pill, no pill points to a stale (version-drifted) guide filename. Scoped hard gate on `ship` |
| `build_delta_routes_guides.py` | Regenerates the `GUIDES` map in `Delta-Routes-SEA.html` from every shipped guide (Step 10 coverage — SEA routing card). Runs on `ship`/`update-index` |
| `validate_delta_routes_guides.py` | Checks `Delta-Routes-SEA.html`'s `GUIDES` map stays in sync with shipped guides on disk. Scoped hard gate on `ship` (Step 10) |
| `validate_guide_country_assignment.py` | Ensures every guide is listed under the correct country block in `Guides-Index.html` — catches a guide added under the wrong country. Hard-blocks ship |
| `validate_guides_index_alphabetical.py` | `Guides-Index.html` must list countries alphabetically, and guides alphabetically within each country. Hard ship-gate, no exceptions |
| `validate_time_zones_cities.py` | Whole-fleet check — every shipped guide has an entry in `Time-Zones.html`'s `CITIES` array (matched via `name:` or `guide:` field). Scoped hard gate on `ship` (Step 9) |
| `validate_us_stats.py` | `Stats-Across-US.html` coverage sweep — every shipped US guide's city is present in EVERY stat category on the page. Scoped hard gate on `ship` (Step 7) |
| `publish_queue.py` | **Serialized publisher** (added 2026-07-05) — `guide_tools.py ship` calls `--enqueue <guide>` then `--drain`. The drain runs under the push lock, reverts the shared root surfaces (Guides-Index / World-Map / Time-Zones) to HEAD, re-applies every queued city idempotently via `guide_surfaces.py`, runs the derived builders, commits, and pushes. Ends "cribs undo each other" — see memory `project_serialized_publisher`. Falls back to `push_queue.py` (scoped push) if the publisher can't run |
| `push_queue.py` | Serializes a single scoped `git push` via a lock file — the fallback path when `publish_queue.py`'s drain can't complete this pass |
| `guide_surfaces.py` | Insert-if-absent appliers used by `publish_queue.py` to re-apply each queued city's card/pin/data entries onto the shared root surfaces without clobbering a neighbouring crib's concurrent additions |
| `crib_safety.py` | Per-guide build lease (`init` claims `Guides/<City>/_build/.lease`, `ship` releases) + `atomic_write` (used by the publisher and all builders so no reader ever sees a half-written page). Check `python3 Brain/scripts/crib_safety.py owned-by-other <Guides/City>` before touching a guide you did not build |
